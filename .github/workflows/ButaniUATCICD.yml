name: Live Pipeline
on:
  push:
    branches:
      - live
    # Trigger at 11 PM IST every day (11 PM IST is 17:30 UTC)
    # - cron: '00 15 * * *'
    
jobs:
  production-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: "1.8"
      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: build spring boot
        run: mvn clean install
      - name: copy file to remote
        uses: appleboy/scp-action@v0.1.7
        with:
          source: "target/jewtrade-service.jar"
          target: /home/azureuser/app-service/
          host: ${{ secrets.AZURE_SSH_HOST }}
          username: ${{ secrets.AZURE_SSH_USER }}
          password: ${{ secrets.AZURE_SSH_PASSWORD }}
          strip_components: 1
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.AZURE_SSH_HOST }}
          username: ${{ secrets.AZURE_SSH_USER }}
          password: ${{ secrets.AZURE_SSH_PASSWORD }}
          port: ${{ secrets.PROD_PORT }}
          script: sudo systemctl restart jewtrade.service
